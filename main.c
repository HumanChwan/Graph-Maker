#include <stdio.h>
#include <stdint.h>

#define SCREEN_FACTOR 50
#define WIDTH (16*SCREEN_FACTOR)
#define HEIGHT (9*SCREEN_FACTOR)

typedef uint32_t Color32;

static Color32 pixels[HEIGHT][WIDTH] = {0};

int main(void) {
    return 0;
}
